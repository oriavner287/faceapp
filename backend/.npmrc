# Security configuration for npm
audit-level=moderate
fund=false

# Security: Prevent automatic execution of scripts
ignore-scripts=false

# Security: Use exact versions for security-critical packages
save-exact=true

# Security: Verify package integrity
package-lock=true